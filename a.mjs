export let name  = `nitan`
export let age = `29`
let address ="gagal"


 export default address

 // export 
 // export by name=> write with curly braces. 
 // export default => curly braces is not needed .
 // In a file we can have multiple export by name but we have only one export by default.
 // if export is done by default the name should not  be same .